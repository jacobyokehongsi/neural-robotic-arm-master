from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project aims to build an algorithm which can control robbotic arm using neural signals.',
    author='Robotic Arm Group (NECL, UCLA)',
    license='MIT',
)
